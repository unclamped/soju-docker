db sqlite3 /var/lib/soju/main.db

# Plain IRC — intended for use behind a TLS-terminating reverse proxy.
# To use TLS directly instead, replace with:
#   listen ircs://
#   tls /etc/soju/cert.pem /etc/soju/key.pem
listen irc://localhost

listen unix+admin://
